version: "3"
services:
  db:
    image: postgres:16-alpine
    container_name: postgres_db
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data


    env_file:
     - postgres.env

volumes:
  postgres_data: